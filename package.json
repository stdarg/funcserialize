{
  "name": "funcserialize",
  "version": "0.0.3",
  "description": "A module to take a function and convert it into an object. And then, with the same object, convert it back into a function. The object is suitable for conversion to JSON via `JSON.stringify`.",
  "main": "index.js",
  "scripts": {
    "test" : "./node_modules/.bin/mocha --reporter list"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/stdarg/funcserialize.git"
  },
  "keywords": [
    "function",
    "serialize",
    "deserialize",
    "JSON"
  ],
  "author": "Edmond Meinfelder",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/stdarg/funcserialize/issues"
  },
  "dependencies": {
    "debug": "~0.7.2",
    "is2": "~0.0.4"
  },
  "devDependencies": {
    "mocha": "~1.13.0"
  }
}
